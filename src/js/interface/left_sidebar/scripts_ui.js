//Initialise functions
{
  function initScriptsUI () { //[WIP] - Finish function body
    //Declare local instance variables
    var common_defines = config.defines.common;
    var scripts_container_selector = common_defines.scripts_container;
    
    console.log(`initScriptsUI() is not yet implemented.`);
  }
}